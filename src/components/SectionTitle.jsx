
const SectionTitle = ({title}) => {
    return (
        <h3 className="my-4 text-lg lg:text-xl uppercase font-medium text-center text-blue-600 tracking-wide">{title}</h3>
    );
};

export default SectionTitle;